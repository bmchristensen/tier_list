<h1>New Tier List</h1>

<%= form_for(@list) do |f| %>
  <%= f.label(:name) %>
  <%= f.text_field(:name) %>
  <br />
  <%= f.label(:champions) %>
  <%= text_area_tag("list[champions]") %>
  <br />
  <%= f.submit %>
<% end %>
