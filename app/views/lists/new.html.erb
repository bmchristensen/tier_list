<h1>New Tier List</h1>

<% if @list.errors.any? %>
<div id="error_explanation">
<h2><%= "#{pluralize(@list.errors.count, "error")}" %> prohibited this list from being saved:</h2>
<ul>
  <% @list.errors.full_messages.each do |message| %>
  <li><%= message %></li>

  <% end %>
<% end %>
<%= form_for(@list) do |f| %>
  <%= f.label(:name) %>
  <%= f.text_field(:name) %>
  <br />
  <%= f.label(:champions) %>
  <%= text_area_tag("list[champions]") %>
  <%# <%= f.text_field(:name) %>
  <br />
  <%= f.submit %>
<% end %>
